## Specification: TASK003
Add app/models/estimate.py and define two Pydantic models: TechnicalEstimateFull with fields estimate_type (Literal['full']), analysis (nested RequirementAnalysis subdocument), milestones (list of Milestone from app/models/project.py), summary (MilestoneProposalSummary from app/models/project.py), model_used (str); TechnicalEstimateDiscovery with fields estimate_type (Literal['discovery']), analysis (nested RequirementAnalysis subdocument), scope_matrix (object with in_scope: list[str] and out_of_scope: list[str]), phase0_hours (int), post_discovery_hourly_rate (float), open_questions (list[str]), model_used (str).
Reuse Milestone, Task, and MilestoneProposalSummary from app/models/project.py instead of redefining them. Example model payload from §6.2: milestones entries contain step (int), name (str), tasks (dict of task name to {description, hours_with_overhead}), hours_with_overhead (int), subtotal (float); summary contains total_hours (int), total_budget (float), delivery_time_weeks (int), hourly_rate_applied (float).