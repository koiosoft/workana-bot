## Specification: TASK006
Add app/database/technical_estimates_repository.py implementing TechnicalEstimatesRepository with two methods: insert(document) persists the Etapa 1+2 accumulated JSON with fields project_id, link_hash, estimate_type ('full'|'discovery'), analysis (nested RequirementAnalysis subdocument), and for branch 'full' the fields milestones (list) and summary; for branch 'discovery' the fields scope_matrix {in_scope,out_of_scope}, phase0_hours, post_discovery_hourly_rate, open_questions; common fields model_used (str), created_at (ISODate); get_latest_by_project_id(project_id) returns the most recent document ordered by created_at DESC. Indexes required: (project_id, created_at DESC) and link_hash. From §6.2: technical_estimates incluye analysis como subdocumento anidado.