## Specification: TASK014
Edit app/bots/telegram/handlers.py to: (1) read contract_type from the project document; (2) if contract_type == 'project_fixed', invoke generate_project_fixed_proposal and persist the resulting RequirementAnalysis into requirement_analyses, the TechnicalEstimate into technical_estimates, and the commercial proposal into proposal_versions (existing collection, source_of_changes='IA'); (3) if contract_type == 'staff_augmentation', keep the current generate_proposal path; (4) Decision #3: staff_augmentation queda igual (sin etapas 2A/2B, sin hitos); (5) Decision #6: la propuesta final sigue en proposal_versions (sin cambios); (6) ensure telemetry messages keep their current structure (no format changes for /analizar, /procesar, /refinar).