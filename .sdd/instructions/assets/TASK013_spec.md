## Specification: TASK013
Edit app/intelligence/factory.py to: (1) update select_initial_proposal_template to return paths with the new subfolder prefix — specifically 's3-commercial/write-proposal.j2' for project_fixed and 's3-commercial/write-proposal-staffing.j2' for staff_augmentation (do NOT include s1-analysis templates here; that function has never returned evaluation or formatter templates); (2) add a select_estimation_template(maturity_score, threshold) helper that returns 's2-estimation/estimate-full.j2' when maturity_score >= threshold, otherwise 's2-estimation/estimate-discovery.j2'.