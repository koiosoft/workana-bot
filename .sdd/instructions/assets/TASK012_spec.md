## Specification: TASK012
Edit app/intelligence/adapters/openrouter.py to: (1) update all get_template() calls to use the new subfolder-prefixed paths; (2) implement analyze_requirement rendering app/intelligence/prompts/s2-estimation/analyze-requirement.j2 and validating the parsed JSON post-hoc against RequirementAnalysis (OpenRouter validation is post-hoc since the provider may not honor response_mime_type); (3) implement estimate_technical selecting s2-estimation/estimate-full.j2 or s2-estimation/estimate-discovery.j2 depending on analysis['branch'] and validating post-hoc against TechnicalEstimateFull or TechnicalEstimateDiscovery; (4) implement write_commercial_proposal rendering s3-commercial/write-proposal.j2 from the validated technical estimate (no numeric recomputation); (5) add generate_project_fixed_proposal orchestrator that calls analyze_requirement → estimate_technical → write_commercial_proposal and returns the commercial proposal JSON; (6) keep generate_proposal (staff aug) and refine_proposal unchanged in behavior. Threshold is read from MATURITY_THRESHOLD env var with default 8.