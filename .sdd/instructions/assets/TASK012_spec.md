## Specification: TASK012
Edit app/intelligence/adapters/openrouter.py to: (1) update all get_template() calls to use the new subfolder-prefixed paths; (2) implement analyze_requirement rendering app/intelligence/prompts/s2-estimation/analyze-requirement.j2 and validating the parsed JSON post-hoc against RequirementAnalysis (OpenRouter validation is post-hoc since the provider may not honor response_mime_type), raising PipelineError on validation failure; (3) implement estimate_technical selecting s2-estimation/estimate-full.j2 or s2-estimation/estimate-discovery.j2 depending on analysis['branch'] and validating post-hoc against TechnicalEstimateFull or TechnicalEstimateDiscovery, raising PipelineError on validation failure; (4) implement write_commercial_proposal rendering s3-commercial/write-proposal.j2 from the validated technical estimate (no numeric recomputation); (5) add generate_project_fixed_proposal orchestrator that calls analyze_requirement → estimate_technical → write_commercial_proposal and RETURNS the full accumulated JSON (analysis + estimate + proposal). Persistence into requirement_analyses, technical_estimates and proposal_versions is NOT the orchestrator's responsibility; the Telegram handler (TASK014) persists the three collections. If analyze_requirement or estimate_technical raises PipelineError, the orchestrator must abort before the write_commercial_proposal (Stage 3 / PREMIUM) call; (6) keep generate_proposal (staff aug) and refine_proposal unchanged in behavior. Threshold is read from MATURITY_THRESHOLD env var with default 8.