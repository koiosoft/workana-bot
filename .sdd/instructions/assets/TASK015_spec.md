## Specification: TASK015
Read MATURITY_THRESHOLD from the environment in a SINGLE place: inside generate_project_fixed_proposal (the Gemini/OpenRouter orchestrator). Do not read it again in the Telegram handler — the handler obtains the value from the orchestrator's returned analysis object (maturity_threshold_used) when persisting to requirement_analyses. Parse the env var to int with default '8'; if the value is present but not a valid integer, raise a clear configuration error instead of silently falling back (per ACK079). Forward the parsed int as maturity_threshold to analyze_requirement, and ensure the value used is recorded in requirement_analyses.maturity_threshold_used for audit (per §6.1 and §8).