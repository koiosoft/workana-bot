## Specification: TASK016
Edit app/bots/telegram/handlers.py to: (1) read contract_type from the project document; (2) if contract_type == 'project_fixed', invoke generate_project_fixed_proposal (which returns the accumulated JSON: analysis + estimate + proposal) and persist three documents: the RequirementAnalysis into requirement_analyses, the TechnicalEstimate (including its nested analysis) into technical_estimates, and the FINAL FLAT MilestoneProposal into proposal_versions (existing collection, source_of_changes='IA'); (3) when writing proposal_versions.proposal_data, write the flat contract the Workana dashboard consumes — proposal_header, milestones, summary, technical_pitch, questions_for_client — NOT the internal nested accumulated shape; milestones and summary carried verbatim from the Stage 2 estimate; (4) if contract_type == 'staff_augmentation', keep the current generate_proposal path unchanged; (5) Decision #3: staff_augmentation queda igual (sin etapas 2A/2B, sin hitos); (6) Decision #6: la propuesta final sigue en proposal_versions (sin cambios estructurales); (7) ensure telemetry messages keep their current structure (no format changes for /analizar, /procesar, /refinar).