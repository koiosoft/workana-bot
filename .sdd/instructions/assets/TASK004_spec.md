## Specification: TASK004
Add app/database/requirement_analyses_repository.py implementing RequirementAnalysesRepository with two methods: insert(document) persists the Etapa 1 accumulated JSON with fields project_id, link_hash, analysis (full nested object: maturity_score, maturity_reason, entities, gaps, branch), maturity_threshold_used (int), model_used (str), created_at (ISODate); get_latest_by_project_id(project_id) returns the most recent document ordered by created_at DESC. Indexes required: (project_id, created_at DESC) and link_hash. From §6.1: Sin versionado secuencial; el último análisis se determina por created_at DESC.