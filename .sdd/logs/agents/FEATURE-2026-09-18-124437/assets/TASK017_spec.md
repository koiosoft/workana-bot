## Specification: TASK017
Verify and consolidate the maturity-threshold wiring produced by TASK012 (the get_maturity_threshold() helper) and TASK013/TASK014 (the adapters that consume it): confirm the helper is the SINGLE source of the threshold, that both adapters call it inside generate_project_fixed_proposal, and that the value actually used is persisted in requirement_analyses.maturity_threshold_used by the handler (TASK016). Fix any remaining inline os.getenv('MATURITY_THRESHOLD') read so there is no duplicated logic. ACK079: when the env var is present but not a valid integer, the helper must raise a clear configuration error rather than silently falling back.