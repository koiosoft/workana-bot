## Specification: TASK013
Edit app/intelligence/adapters/gemini.py to: (1) implement analyze_requirement rendering app/intelligence/prompts/s2-estimation/analyze-requirement.j2 with response_mime_type='application/json' (Gemini lo admite nativamente como respuesta estructurada) and validating the parsed JSON against RequirementAnalysis via model_validate, raising PipelineError on validation failure; (2) implement estimate_technical rendering s2-estimation/estimate-full.j2 or s2-estimation/estimate-discovery.j2 depending on analysis['branch'] with response_mime_type='application/json' and validating against TechnicalEstimateFull or TechnicalEstimateDiscovery, raising PipelineError on validation failure; (3) implement write_commercial_proposal rendering s3-commercial/write-proposal.j2 from the validated technical estimate (no numeric recomputation); (4) add generate_project_fixed_proposal orchestrator that calls analyze_requirement → estimate_technical → write_commercial_proposal and RETURNS the full accumulated JSON (analysis + estimate + proposal). Persistence into requirement_analyses, technical_estimates and proposal_versions is NOT the orchestrator's responsibility; the Telegram handler (TASK016) persists the three collections. If analyze_requirement or estimate_technical raises PipelineError, the orchestrator must abort before the write_commercial_proposal (Stage 3 / PREMIUM) call; (5) keep generate_proposal (staff aug) and refine_proposal unchanged in behavior. Inside generate_project_fixed_proposal call the get_maturity_threshold() helper from app.intelligence.config and pass the returned int as maturity_threshold to analyze_requirement. The template path references in this file were already updated by TASK002 — do not re-run that task.