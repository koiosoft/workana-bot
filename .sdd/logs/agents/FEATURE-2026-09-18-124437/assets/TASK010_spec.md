## Specification: TASK010
Edit app/intelligence/prompts/s3-commercial/write-proposal.j2 so it is 100% commercial writing and NOTHING else (Decision #9). Input is technical_estimate_json (the Stage 2 output containing milestones and summary for a 'full' branch). The Stage 3 template ONLY creates the persuasive prose fields: proposal_header (str), technical_pitch (str), questions_for_client (list[str]). It must ALSO emit the complete final MilestoneProposal JSON required by the Workana dashboard — that is the contract the external dashboard consumes and it MUST NOT be altered: the output object must contain exactly proposal_header, milestones, summary, technical_pitch, questions_for_client. The milestones and summary are carried through VERBATIM from technical_estimate_json (including each milestone name, task descriptions, hours_with_overhead, subtotal, and all summary fields) — do NOT recompute, rename, restyle, summarize or drop them. Omitting milestones or summary from the output breaks the external dashboard contract. Maintain the existing \\n\\n line-break maquetation and the conditional CTA on technical_pitch. The estimation logic that lived in proposal.j2 (270h minimum, technical floors, milestone construction) is NOT part of this template; it belongs to estimate-full.j2.