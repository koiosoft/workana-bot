## Current Objective
Migrate the existing Next.js project API endpoints to a new Python REST API to centralize backend logic and prepare for new features, based on the analysis in `PYTHON-MIGRATION-NEXTJS-API.md`.

## Current Task
- [] **Establish Database Connection**: Review the to exist reusable module within the Python application to handle the connection to the MongoDB database. This module should read the connection URI from an environment variable and provide a single, accessible client instance for other parts of the application to use for database operations. Review the implementation to fix some problems only if necessary.

## Task List
- [x] **Define Project Data Models**: Create the necessary Pydantic models in Python to accurately represent the project information stored in MongoDB. This involves creating models that mirror the fields described in the migration plan, such as title, description, status, skills, and the different proposal types. Ensure all fields and their corresponding data types match the existing schema for `projects`.
- [] **Establish Database Connection**: Review the to exist reusable module within the Python application to handle the connection to the MongoDB database. This module should read the connection URI from an environment variable and provide a single, accessible client instance for other parts of the application to use for database operations. Review the implementation to fix some problems only if necessary.
- [ ] **Implement Project Listing Endpoint (`GET /projects`)**: Create an API endpoint that retrieves a list of projects. This endpoint must replicate the complex filtering, sorting, and pagination logic from the original Next.js `getProjects` service function. This includes filtering by proposal status (`status`), searching by a term (`searchTerm`), handling the `staffAugmentationOnly` flag, and sorting projects based on their estimated publication date, AI score, and update time. The JSON response must match the original structure, providing a list of projects and the total count of documents matching the query.
- [ ] **Implement Project Detail Endpoint (`GET /projects/{id}`)**: Create an API endpoint to retrieve a single project by its unique identifier. The endpoint will receive the project ID as part of the URL path. It needs to validate that the provided ID is a valid MongoDB ObjectId format, query the database for the corresponding project, and return the project's data in the same JSON format as the original `/api/projects/[id]` endpoint.
- [ ] **Implement Project Update Endpoint (`PATCH /projects/{id}`)**: Create an API endpoint to update specific fields of a project. This endpoint will receive the project ID in the URL and a JSON object with the fields to update in the request body. It must validate the ID, apply the changes to the correct project document using a `$set` operation, and return a confirmation message upon success. It must also handle and report errors for invalid IDs or when a project is not found.
