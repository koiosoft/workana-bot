## Current Objective
Create a new API endpoint at `/api/proposals/{proposalId}/refine` that accepts an LLM model ID and observations, generates a refined proposal using the selected model, and stores the result in the `proposal_versions` collection with `source_of_changes` set to "IA". Also, generate unit and integration tests for the new endpoint.

## Key Artifacts (to focus on)
- **Files**:  
  - `app/api/routes/projects.py`  
  - `app/database/proposal_versions_repository.py`  
  - `app/intelligence/port.py`  
  - `app/intelligence/factory.py`  
  - `app/intelligence/adapters/openrouter.py`  
  - `app/intelligence/prompts/refine.j2` (new file)  
  - `tests/unit/api/test_projects.py` (new file)  
  - `tests/integration/api/test_proposals.py` (new file)  
- **Classes/Interfaces**:  
  - `IntelligencePort`  
  - `ProposalVersionsRepository`  
  - `OpenRouterAdapter`  
  - `get_intelligence_service`  
- **Configuration**:  
  - `OPENROUTER_API_KEY` (environment variable)

## Task List
- [x] Read `app/api/routes/projects.py` and `app/database/proposal_versions_repository.py` to understand how the `/api/projects/{projectId}` endpoint and the `proposal_versions` collection are handled, then create a new endpoint at `/api/proposals/{proposalId}/refine` that accepts a POST request with `llm_model_id` and `user_feedback_observations`, processes the request, and returns a response in the same format as `/api/projects/{projectId}`.
- [x] Read `app/intelligence/port.py` and `app/intelligence/adapters/openrouter.py` to understand how the `IntelligencePort` interface and the `OpenRouterAdapter` class are implemented, then create a new method in `OpenRouterAdapter` named `refine_proposal` that takes a `project` dictionary, `user_feedback_observations` string, and `model_id`, and generates a refined proposal using the specified model.
- [x] Read `app/intelligence/factory.py` and modify the `get_intelligence_service` function to include the `refine_proposal` method in the adapter's interface, ensuring it is called with the correct parameters when invoked.
- [x] Read `app/intelligence/prompts/refine.j2` (if it exists) or create a new file `app/intelligence/prompts/refine.j2` with a Jinja2 template that generates a prompt for refining a proposal, including the `user_feedback_observations` parameter.
- [x] Modify the new `/api/proposals/{proposalId}/refine` endpoint to render the `refine.j2` template with the provided `user_feedback_observations`, then use the appropriate `IntelligencePort` adapter to generate the refined proposal.
- [x] After generating the refined proposal, use the `ProposalVersionsRepository` class to insert a new version into the `proposal_versions` collection, ensuring the `source_of_changes` field is set to "IA".
- [x] Extract the "refinement_justification" field from the root of the JSON generated by the LLM and inject it as a top-level field into the document being inserted into the "proposal_versions" collection. Crucially, the "proposal" attribute of that document must contain exclusively the inner "proposal" object generated by the LLM, meaning the saved proposal data will not contain the "refinement_justification" field inside it.
- [x] Create a new unit test file `tests/unit/api/test_projects.py` that includes test cases for the new `/api/proposals/{proposalId}/refine` endpoint, using mocks for the `IntelligencePort` and `ProposalVersionsRepository` to verify the logic.
- [x] Create a new integration test file `tests/integration/api/test_proposals.py` that includes test cases for the new `/api/proposals/{proposalId}/refine` endpoint, ensuring it interacts correctly with the database and handles real LLM model requests using the `OPENROUTER_API_KEY` environment variable.

## End Task List